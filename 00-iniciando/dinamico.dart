void main(List<String> args) {
  // a variavel var pode ser String, numero, lista e bool
  var nome;
  nome = 'yago';

  // dynamic é um tipo sem definição, como o var
  dynamic ano;
  ano = 2019;
  print(nome + ano);
}